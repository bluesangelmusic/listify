import styled from 'styled-components'

/**
 * A custom wrapper `<div>` for form inputs and their labels.
 */
export const FormGroup = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  margin-bottom: 1em;
`
