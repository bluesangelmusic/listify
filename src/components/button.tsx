import styled from 'styled-components'

/**
 * A generic `<button>` element styled to match the app.
 */
export const Button = styled.button`
  background: white;
  color: #001659;
  border: 1px solid #001659;
  padding: 0.5em 1em;
  cursor: pointer;
`
